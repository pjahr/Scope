<UserControl x:Class="Scope.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Scope.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:SearchViewModel, IsDesignTimeCreatable=False}">
    <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <TextBox Text="{Binding SearchTerms}"
             Margin="3"
             Padding="3"/>

    <Button Content="Find" Command="{Binding FindFilesBySearchTermsCommand }"
            Grid.Column="1"
            Margin="3"
            Padding="3"
            />

    <TextBlock Text="UNDER  CONSTRUCTION&#10;(mockup)" 
               Foreground="Black"
               Background="#99dddd00"
               TextAlignment="Center"
               FontSize="10"
               Grid.Row="0"
               Grid.RowSpan="4"
               Grid.Column="0"
               Grid.ColumnSpan="2"
               Padding="3"
               Margin="3"
               VerticalAlignment="Bottom"
               HorizontalAlignment="Right"
               >      

    </TextBlock>

    <WrapPanel
      Grid.Row="1"
             Grid.ColumnSpan="2"
            Margin="3" 
      >
      <DockPanel Margin="3 3 12 3">
        <CheckBox x:Name="_checkboxFilename"
                  DockPanel.Dock="Left"
                  VerticalAlignment="Center"/>
        <Label Target="{Binding ElementName =_checkboxFilename}"
               Content="File name"
               DockPanel.Dock="Right"/>
      </DockPanel>
    
    <DockPanel Margin="3">
      <CheckBox x:Name="_checkboxCaseSensitive"
                  DockPanel.Dock="Left"
                  VerticalAlignment="Center"/>
      <Label Target="{Binding ElementName=_checkboxCaseSensitive}"
               Content="Case sensitive"
               DockPanel.Dock="Right"/>
    </DockPanel>
    </WrapPanel>

    <TextBlock Text="File types"
                 Grid.Row="2"
               Margin="6"/>

    <WrapPanel
      Grid.Row="3"
             Grid.ColumnSpan="2"
            Margin="3 0" >
      <DockPanel Margin="3 3 12 3">
        <CheckBox DockPanel.Dock="Left"
                  VerticalAlignment="Center"/>
        <Label Content="JSON"
               DockPanel.Dock="Right"/>
      </DockPanel>

      <DockPanel Margin="3">
        <CheckBox DockPanel.Dock="Left"
                  VerticalAlignment="Center"/>
        <Label Content="XML"
               DockPanel.Dock="Right"/>
      </DockPanel>

      <DockPanel Margin="3">
        <CheckBox DockPanel.Dock="Left"
                  VerticalAlignment="Center"/>
        <Label Content="ID"
               DockPanel.Dock="Right"/>
      </DockPanel>

      <DockPanel Margin="3">
        <CheckBox DockPanel.Dock="Left"
                  VerticalAlignment="Center"/>
        <Label Content="DCB"
               DockPanel.Dock="Right"/>
      </DockPanel>

      <DockPanel Margin="3">
        <CheckBox DockPanel.Dock="Left"
                  VerticalAlignment="Center"/>
        <Label Content="TXT"
               DockPanel.Dock="Right"/>
      </DockPanel>

    </WrapPanel>

  </Grid>
  
</UserControl>
